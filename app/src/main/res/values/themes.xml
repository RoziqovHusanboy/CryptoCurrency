<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Certificate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.Certificate" parent="Base.Theme.Certificate" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/color_splashscreen</item>
        <item name="windowSplashScreenAnimatedIcon">@string/splash_screen_title</item>
        <item name="postSplashScreenTheme">@style/Theme.Certificate</item>

    </style>
</resources>